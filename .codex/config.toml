# Project-scoped Codex settings. Provider definitions remain machine-local;
# see planning/phase102_native_codex_orchestration.md.

[agents]
# Root Coordinator (depth 0) -> Supervisor (depth 1) -> Worker (depth 2), with
# one additional nesting level available for recursive delegation probes.
max_depth = 3
max_threads = 3
