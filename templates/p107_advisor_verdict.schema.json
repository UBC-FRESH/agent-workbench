{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["review_number", "verdict", "deterministic_acceptance", "correctness", "score", "critical_defects", "defect_packet"],
  "properties": {
    "review_number": {"type": "integer", "minimum": 1, "maximum": 3},
    "verdict": {"enum": ["accepted", "defect_packet", "verified_blocker"]},
    "deterministic_acceptance": {"type": "boolean"},
    "correctness": {"type": "integer", "minimum": 0, "maximum": 4},
    "score": {"type": "integer", "minimum": 0, "maximum": 10},
    "critical_defects": {"type": "array", "items": {"type": "string"}},
    "defect_packet": {"type": ["object", "null"]}
  },
  "allOf": [
    {"if": {"properties": {"verdict": {"const": "accepted"}}}, "then": {"properties": {"deterministic_acceptance": {"const": true}, "correctness": {"minimum": 3}, "score": {"minimum": 8}, "critical_defects": {"maxItems": 0}, "defect_packet": {"const": null}}}},
    {"if": {"properties": {"verdict": {"const": "defect_packet"}}}, "then": {"required": ["defect_packet"]}},
    {"if": {"properties": {"verdict": {"const": "verified_blocker"}}}, "then": {"properties": {"defect_packet": {"const": null}}}}
  ]
}
